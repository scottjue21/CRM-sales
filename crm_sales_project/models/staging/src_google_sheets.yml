version: 2

sources:
  - name: google_sheets
    database: DPS_ANALYTICS
    schema: GOOGLE_SHEETS
    quoting:
      database: false
      schema: false
      identifier: true
    tables:
      - name: sales_pipeline
        identifier: SALES_PIPELINE
        freshness:
          warn_after:
              count: 2
              period: hour
          error_after:
              count: 2
              period: hour
        loaded_at_field: _airbyte_extracted_at
        columns:
          - name: opportunity_id
            tests:
              - unique
              - not_null